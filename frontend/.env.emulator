# Used by `npm run dev:emulators` and the tests: talks to the local Firebase emulators.
# "demo-" project IDs never touch a real Firebase project.
VITE_FIREBASE_API_KEY=demo-api-key
VITE_FIREBASE_PROJECT_ID=demo-household
VITE_USE_EMULATORS=true
